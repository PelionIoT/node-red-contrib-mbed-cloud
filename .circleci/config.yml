version: 2
jobs:
  build:
    docker:
      - image: node:8.0
    steps:
      - checkout 
      - run:
          name: Install Node-RED
          command: docker run -it -p 1880:1880 --name mynodered nodered/node-red-docker